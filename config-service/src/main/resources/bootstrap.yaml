server:
  port: 8040

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: https://github.com/wilder06/microservicios-cloud.git
          search-paths: config-data
          default-label: develop

