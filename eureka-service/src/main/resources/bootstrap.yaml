spring:
  application:
    name: eureka-service

server:
  port: 8761

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false

  cloud:
   config:
      enabled: true
      uri: http://config-service:8040

